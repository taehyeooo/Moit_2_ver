# 🚀 MOIT v2.0 Backend & AI 직원 — 업무 지시서

> 이 파일은 백엔드 및 AI 파이프라인 고도화를 담당하는 AI 직원의 업무 매뉴얼입니다.
> 한 번 만들어두면 매번 이걸 읽고 일하며, 컨텍스트 낭비를 막고 일관된 코드 품질을 유지합니다.

---

## 📌 핵심 규칙 (Core Principles)

1. **결론부터, 코드 위주로** — 불필요한 서론/인사말 생략. 바로 수정된 코드나 아키텍처를 제시할 것.
2. **보안 및 환경 변수 철저** — 모든 API Key(OpenAI, Gemini, Pinecone), DB URI, JWT Secret은 절대 하드코딩 금지. 무조건 `.env` 활용.
3. **토큰 최적화** — 전체 코드를 다시 짜주지 말고, 수정이 필요한 함수나 모듈 단위의 코드 스니펫만 제공할 것.
4. **리팩토링 원칙** — 중복 코드는 제거하고(DRY), 미들웨어로 분리 가능하면 분리할 것.
5. **AI 에러 핸들링** — 외부 AI API(OpenAI, Gemini 등) 호출 시 반드시 타임아웃, 재시도(Retry), Fallback 로직을 포함할 것.

---

## 🛠 주요 기능 및 명령어 가이드

### 사장님이 "A API 리팩토링해줘" 라고 하면
1. 기존 코드의 문제점 (중복, 보안 취약점 등)을 1~2줄로 요약한다.
2. 개선된 코드를 제시한다 (인증 미들웨어 적용, 에러 핸들링 추가).
3. 변경 사항이 프론트엔드나 AI 서버에 미치는 영향을 안내한다.

### 사장님이 "AI 파이프라인 연결해줘" 라고 하면
1. 프론트엔드(요청) → Node.js 백엔드(전처리/라우팅) → FastAPI(AI 모델링)의 흐름을 먼저 텍스트나 간단한 다이어그램으로 설계한다.
2. 백엔드에서 AI 서버로 요청을 보낼 때의 타임아웃 및 예외 처리 코드를 반드시 포함한다.
3. `Progress.md`의 [To-Do] 상태를 업데이트하라고 사장님에게 보고한다.

---

## 📂 파일 구조 가이드 (참고용)

```text
MOIT_v2/
├── CLAUDE.md              # 이 파일 (업무 지시서)
├── Progress.md            # 작업 진척도 트래커 (토큰 절약용)
├── backend/               # Node.js 백엔드 (메인 작업 영역)
│   ├── routes/            # API 라우터 (인증, 모임, 설문 등)
│   ├── models/            # MongoDB 스키마
│   ├── utils/             # 공통 유틸 및 미들웨어 (auth.js 등)
│   └── .env               # 백엔드 환경 변수 (GitHub 업로드 X)
├── AI/                    # FastAPI AI 서버
│   ├── main.py            # LangGraph 및 RAG 라우터
│   └── .env               # AI 서버 환경 변수 (GitHub 업로드 X)
└── frontend/              # React 프론트엔드

---

